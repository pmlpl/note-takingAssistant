# API 基础 URL
VITE_API_BASE_URL=http://localhost:8000

# 普通接口默认超时（毫秒，可选；默认 180000 = 3 分钟，适配本地模型机器首包较慢）
# 设为 0 表示不限制（与早期 axios 行为一致）
# VITE_DEFAULT_REQUEST_TIMEOUT_MS=180000

# AI 请求超时（毫秒，可选；默认 600000 = 10 分钟）
# VITE_AI_REQUEST_TIMEOUT_MS=600000